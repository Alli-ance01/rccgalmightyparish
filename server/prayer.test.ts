import { beforeEach, describe, expect, it, vi } from "vitest";
import type { TrpcContext } from "./_core/context";
const dbMock = vi.hoisted(() => ({ createPrayerRequest: vi.fn(), listPrayerRequests: vi.fn(), updatePrayerRequestStatus: vi.fn() }));
vi.mock("./db", () => dbMock);
import { prayerRouter } from "./routers/prayer";
function adminContext(): TrpcContext { return { user: { id: "507f1f77bcf86cd799439011", openId: "tap-admin", name: "Administrator", email: "admin@tapchurch.org", passwordHash: "hash", accountType: "admin", accountStatus: "active", role: "admin", createdAt: new Date(), updatedAt: new Date(), lastSignedIn: new Date() }, req: { headers: {} } as TrpcContext["req"], res: {} as TrpcContext["res"] }; }
describe("prayer request router", () => { beforeEach(() => vi.clearAllMocks()); it("accepts a public prayer request", async () => { dbMock.createPrayerRequest.mockResolvedValue("507f1f77bcf86cd799439012"); await expect(prayerRouter.createCaller({ ...adminContext(), user: null }).submit({ request: "Please pray for wisdom and strength in this season.", wantsFollowUp: false })).resolves.toBe("507f1f77bcf86cd799439012"); }); it("allows administrators to review confidential requests", async () => { dbMock.listPrayerRequests.mockResolvedValue([]); await expect(prayerRouter.createCaller(adminContext()).list({ status: "new" })).resolves.toEqual([]); expect(dbMock.listPrayerRequests).toHaveBeenCalledWith("new"); }); });
